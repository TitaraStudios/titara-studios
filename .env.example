# Public contact endpoints (optional) — used by the contact form integration.
# Copy this file to `.env` and fill in real values when wiring a backend.
VITE_CONTACT_ENDPOINT=
VITE_GOOGLE_MAPS_EMBED_URL=
